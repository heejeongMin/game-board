package com.pancho.game.domain

enum class GameScoring(name: String) {
    TOTAL_SCORE_LOWEST("total score lowest"),
    TOTAL_SCORE_HIGHEST("total score highest"),
    WINNING_COUNT("winning count");
}